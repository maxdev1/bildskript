bildskript 0.1
--------------

This is the first release of bildskript, a visual program interpreter.
The "specification/bildskript.png" contains how to draw the components,
functions (chips) are not implemented yet.

Open the "bildskript.jar" to run the program. The "sources.zip" file
contains the source code.

Editor keys:
	1		Draw component parts (red)
	2		Draw lanes (black)
	3		Draw lane skips (gray)
	4		Draw white
	s		Start/stop selection/action
	x		Cut selected area
	c		Copy selected area
	v		Paste

Have fun!
maxdev